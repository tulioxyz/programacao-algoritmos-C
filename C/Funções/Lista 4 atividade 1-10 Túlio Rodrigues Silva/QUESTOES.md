# Lista 04

1. Crie a função x_ehDigito que verifique se um caractere (char) fornecido é um
dígito ('0' a '9'). Elabore um programa que leia caracteres do teclado e utilizando
a função para verificar, exiba se o caractere informado ó ou não um digito.
-----------------------------------------------------------------------------------
2. Desenvolva a função x_paraMaiscula que transforma um caractere (char)
minúsculo em sua correspondente maiúscula.
Dica: observe como os caracteres são organizados na tabela ASCII e como isso
permite fazer conversões matemáticas simples.
-----------------------------------------------------------------------------------
3. Implemente a função pot(float x, int n) que calcula x
n
, retornando um valor do

tipo float.
-----------------------------------------------------------------------------------
4. Indique se as seguintes afirmações são verdadeiras (V) ou falsas (F):

a. ( ) Uma função pode devolver mais de um valor.
b. ( ) Uma função pode não ter parâmetros.
c. ( ) Toda função deve retornar um inteiro.
d. ( ) Os parâmetros de uma função podem ser do tipo void.
e. ( ) A instrução return finaliza a execução de uma função.
f. ( ) Variáveis locais podem ter o mesmo nome que os parâmetros.
g. ( ) O return só finaliza a função se estiver no fim do bloco.
h. ( ) return em qualquer função finaliza o programa.
i. ( ) return em main finaliza o programa.
j. ( ) O nome de uma função é opcional.
k. ( ) Parâmetros de função são opcionais.
l. ( ) Uma função deve realizar o máximo de tarefas possível.
m. ( ) Funções devem ter no máximo 10 linhas.
n. ( ) O nome da função deve ter no máximo 6 letras.
o. ( ) Não se pode usar palavras reservadas como nome de função.
-----------------------------------------------------------------------------------
5. Dadas as funções Ping e Pong,
determine a saída para (Teste de Mesa):
a) Pong(3)
b) Ping(-4)
c) Ping(25)
d) Pong(2)
e) Pong(1)
-----------------------------------------------------------------------------------
6. Implemente a função int abs(int x) que devolve o valor absoluto de um número.
O valor absoluto de um número é a sua distância em relação a zero na reta
numérica, e é sempre um valor positivo. Por exemplo, o valor absoluto de +5
e -5 é 5, pois ambos estão a 5 unidades de distância do zero.
-----------------------------------------------------------------------------------
7. Implemente a função long int n_segundos(int n_horas) que retorna a
quantidade de segundos correspondente a n_horas.
-----------------------------------------------------------------------------------
8. Desenvolva a função float max(float x, float y, float w) que retorna o maior dos
três valores.
-----------------------------------------------------------------------------------
9. Crie a função int entre(int x, int lim_inf, int lim_sup) que verifica se x está dentro
do intervalo [lim_inf, lim_sup].
-----------------------------------------------------------------------------------
10.Desenvolva a função int eh_quadrado(int x, int y) que verifica se x é igual a y2.